<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.promptlm</groupId>
        <artifactId>promptlm-dependencies</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../promptlm-dependencies/pom.xml</relativePath>
    </parent>

    <artifactId>promptlm-parent</artifactId>
    <packaging>pom</packaging>

    <name>promptLM Parent</name>
    <description>
        Build-policy parent for promptLM repositories. Extends promptlm-dependencies
        (inheriting all dependency management) and adds compiler/plugin policy,
        shared test dependencies, and the dynamic-agent-loading profile.
        Consumers that already have their own parent should import
        promptlm-dependencies directly instead. See ADR 0004 in
        promptLM/promptlm-test-support.
    </description>
    <url>https://github.com/promptLM/promptlm-platform</url>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.scm.id>github</project.scm.id>

        <!-- Surefire wiring: argLine for module-overridable JVM args; api.key wires the OpenAI key into tests. -->
        <api.key>${env.OPENAI_API_KEY}</api.key>
        <argLine></argLine>
        <surefire.extraArgs></surefire.extraArgs>

        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.5.1</maven-failsafe-plugin.version>
        <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.10.1</maven-javadoc-plugin.version>
        <maven-release-plugin.version>3.3.1</maven-release-plugin.version>
        <maven-deploy-plugin.version>3.1.3</maven-deploy-plugin.version>
        <spring-boot-maven-plugin.version>${spring-boot.version}</spring-boot-maven-plugin.version>
        <native.maven.plugin.version>0.11.5</native.maven.plugin.version>

        <spotless-maven-plugin.version>2.46.1</spotless-maven-plugin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit-pioneer</groupId>
            <artifactId>junit-pioneer</artifactId>
            <version>2.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot-maven-plugin.version}</version>
                    <configuration>
                        <skip>true</skip>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.graalvm.buildtools</groupId>
                    <artifactId>native-maven-plugin</artifactId>
                    <version>${native.maven.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless-maven-plugin.version}</version>
                    <configuration>
                        <java>
                            <includes>
                                <include>src/main/java/**/*.java</include>
                                <include>src/test/java/**/*.java</include>
                            </includes>
                            <!--
                                Light-touch rules only for 0.1.0. We deliberately do NOT
                                enable googleJavaFormat / palantirJavaFormat here — they
                                would rewrite essentially every existing file. The header
                                check stays out of Spotless because it is already enforced
                                by the pre-commit hook (Lucas-C/insert-license) and the
                                apache/skywalking-eyes/header GitHub Action in CI.
                                Revisit adopting a formatter in a dedicated PR for 0.2.0.
                            -->
                            <removeUnusedImports/>
                            <importOrder/>
                        </java>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>${maven-release-plugin.version}</version>
                <configuration>
                    <tagNameFormat>v@{project.version}</tagNameFormat>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <argLine>${argLine} ${surefire.extraArgs}</argLine>
                    <systemPropertyVariables>
                        <apiKey>${api.key}</apiKey>
                        <junit.jupiter.tags.exclude>manual</junit.jupiter.tags.exclude>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <!--
                Spotless is configured in <pluginManagement> above but NOT bound
                to the default lifecycle for 0.1.0. Default `mvn verify` does
                not run Spotless. To check formatting on demand:
                    mvn -Pspotless verify        # check, fail on violations
                    mvn spotless:check           # check ad-hoc
                    mvn spotless:apply           # apply fixes locally
                The opt-in `spotless` profile is declared below.

                License header enforcement is handled by:
                  - Pre-commit hook (Lucas-C/insert-license) for developer-loop
                    insertion (.pre-commit-config.yaml)
                  - apache/skywalking-eyes/header GitHub Action in CI
                    (.github/workflows/oss-checks.yml + .licenserc.yaml)
                There is no stable skywalking-eyes Maven plugin on Central,
                so the action is the authoritative cross-language check.
            -->
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>github</id>
        </profile>
        <profile>
            <id>enable-dynamic-agent-loading</id>
            <activation>
                <jdk>[21,)</jdk>
            </activation>
            <properties>
                <surefire.extraArgs>-XX:+EnableDynamicAgentLoading</surefire.extraArgs>
            </properties>
        </profile>

        <!--
            Opt-in formatting check. Activate with `-Pspotless` or by setting
            -Dspotless.check=true. Off by default so existing modules continue
            to build until they are individually brought into formatting
            compliance.
        -->
        <profile>
            <id>spotless</id>
            <activation>
                <property>
                    <name>spotless.check</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.diffplug.spotless</groupId>
                        <artifactId>spotless-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>spotless-check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
