<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.promptlm</groupId>
    <artifactId>promptlm-bom</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>promptLM BOM</name>
    <description>
        Pure bill-of-materials for the promptLM platform release train. Anchors
        Spring Boot 3.5.x and any other PromptLM-wide dependency versions.
        Consumers import via &lt;scope&gt;import&lt;/scope&gt; — no build policy, no
        plugin configuration. See ADR 0004 in promptLM/promptlm-test-support.
    </description>
    <url>https://github.com/promptLM/promptlm-platform</url>

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:https://github.com/promptLM/promptlm-platform.git</connection>
        <developerConnection>scm:git:https://github.com/promptLM/promptlm-platform.git</developerConnection>
        <url>https://github.com/promptLM/promptlm-platform</url>
    </scm>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/promptLM/promptlm-platform</url>
        </repository>
        <snapshotRepository>
            <id>github</id>
            <name>GitHub Packages Snapshots</name>
            <url>https://maven.pkg.github.com/promptLM/promptlm-platform</url>
        </snapshotRepository>
    </distributionManagement>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--
            Spring Boot 3.5.x is the anchor for the 1.x train. The Boot 3 -> 4
            migration is planned as promptlm-platform:2.0.0 (see ADR 0004 D2).
        -->
        <spring-boot.version>3.5.4</spring-boot.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM: transitively pins Jackson, SLF4J, Logback, Jetty, etc. -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
